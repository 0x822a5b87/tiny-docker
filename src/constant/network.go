package constant

// BaseCidr The base CIDR is a typical convention-over-configuration (CoC) CIDR that Docker uses as the base CIDR.
const BaseCidr = `172.17.0.0/16`

const SizeOfSubnet uint64 = 64
const SizeOfSubnetIp uint64 = 65536
